const { getTicketByName, calculateTotalFromTicketNames} = require('../src/tickets.js')

// @returns The ticket object with a matching ID or `null` if no object is found.


// @returns The total value of all tickets from the `names` array or 0

